<p style="color: green"><%= notice %></p>

<%= render @project %>
<h4><%= @project.name %>'s description</h4>
<% @tasks.each do |task|%>
  <li><%= task.description %></li>
<% end %>
<br>
<div>
  <%= link_to "Edit", edit_project_path(@project) %> |
  <%= link_to "Back", projects_path %>
</div>
<br>
<div>
  <%= button_to "Destroy", @project, method: :delete %>
</div>

